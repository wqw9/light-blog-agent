# 上线前全量自动化回归测试（配置与文档）

**日期**：2026-08-15

## 内容
- 新增 `scripts/prelaunch-test.mjs` 上线前全量回归脚本：
  - 公开接口 19 项：站点/关于/小人配置、文章列表、搜索、标签筛选、可见性保护、章节渲染与高亮、阅读计数与时长上报、书注公开读写、未授权删除拦截、Live2D 模型加载、上传文件静态保护、路径穿越、安全响应头、CORS、会话/用量/上传记录 401
  - 管理接口 15 项（需 `MYBLOG_ADMIN_PASSWORD`）：上传记录、LLM 脱敏、book-finder Skill、站点背景保存与还原、baseUrl 校验、私密/草稿创建与可见性闭环（404/搜索不泄漏/排行不泄漏/管理员可读）、书注删除、更新+撤销、标签颜色校验、上传自动出版+受控下载
  - 限流爆破 2 项（仅隔离实例，`RISKY=1`）：口令爆破第 6 次 429、聊天第 21 次 400
- 测试方法：隔离实例（:3001 + 数据库副本 + 注入测试口令），不影响真实数据；全部用例自动清理测试产物
- 新增 `update/配置与文档/2026-08-15-上线前检查清单.md`：显示与交互人工检查清单（全局/小人/书架/阅读/统计/关于/管理/部署 8 类）

## 结果
- 隔离实例全量回归：**36/36 通过，0 失败**
- 服务端 tsc 全量类型检查通过；全部 Vue 组件 SFC 编译通过

## 提醒
- 上线前在真实环境再跑一遍：重启后端最新代码 → `corepack pnpm build` → `node scripts/verify-features.mjs` + `node scripts/prelaunch-test.mjs`（均带口令，不设 RISKY）→ 按人工清单过一遍显示项
