# 初始化清理与双语 README（配置与文档）

**日期**：2026-08-15

## 内容
- **全部设置初始化（无关键信息）**：admin.json 口令清空、llm.json 移除 apiKey 且 enabled=false、about.json 还原模板（移除个人 GitHub/邮箱）、mascot.json 还原默认
- **敏感数据清理**：删除 data/secret.key（加密密钥）、含解密口令的临时文件、全部日志文件、临时诊断脚本
- **.gitignore 更新**：新增忽略 `data/secret.key`、`uploads/models/*`（仅保留 README）、`*.log`、`.tmp-*`
- **README 全面重写（中英双语）**：整体项目内容 / AI 生成 / 技术栈 / 快速开始 / 如何使用 / 部署方法（含 Caddy 反代示例）/ 配置说明 / 项目结构 / API 速览 / 验证
- **更新记录归档**：建立 update/ 目录，按"功能更新 / 修复记录 / 配置与文档"分类存放每次更新的提醒内容

## 提醒
- 本地 Live2D 模型文件仍保留在 uploads/models/（gitignore 不影响磁盘文件与服务），已恢复 mascot.json 的 localModelPath 指向
- LLM 处于关闭状态，使用前到管理页重新填 apiKey（自动加密）
