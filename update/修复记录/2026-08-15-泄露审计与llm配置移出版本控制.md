# 泄露风险审计与 llm.json 移出版本控制（修复记录）

**日期**：2026-08-15

## 审计结论（git 全历史扫描）
- ✅ 真实管理口令、scrypt 哈希、LLM API Key、加密密钥 secret.key、数据库、.env、上传的个人文件：**从未进入 git 历史**（历史中匹配到的 "scrypt$"/"apiKeyEnc" 均为源码与文档字符串）
- ✅ 已提交的 admin.json 为空模板（`{"password":""}`），后已移出版本控制
- ⚠️ 提交者邮箱为真实邮箱（公开可见）：建议未来提交改用 GitHub noreply 邮箱（见下）

## 本次修复
- **config/llm.json 移出版本控制**：改名为 `config/llm.json.example`（模板入库），运行时文件 `config/llm.json` 加入 .gitignore —— 以后在管理页填 API Key（自动加密写入该文件）不会再被 `git add -A` 误提交
- ConfigService 兼容文件缺失：未配置时返回安全默认结构（禁用、无密钥），保存时自动创建
- setup-server.sh / clean-local-data.ps1 同步适配（模板复制与清理逻辑）
- README 配置表更新

## 建议（用户侧）
- 未来提交使用 GitHub 隐私邮箱：`git config user.email "wqw9@users.noreply.github.com"`（GitHub 头像旁也建议关闭"公开我的邮箱"）
- 已推送的旧提交邮箱如需抹除，可做历史重写（会改变全部 commit SHA，需 force-push；服务器尚未部署，现在是做这件事的最佳时机，需要的话我可以给出步骤）
