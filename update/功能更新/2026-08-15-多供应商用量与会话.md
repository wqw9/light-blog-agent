# 多供应商 · 用量 · 会话 · 气泡

**日期**：2026-08-15

## 内容
- **多 LLM 接口**：providers 结构（每供应商独立 baseUrl/模型/视觉模型/入出价格/apiKey），activeProvider 切换，支持添加自定义接口，旧配置启动时自动迁移
- **每日 Token 限额 + 花费统计**：token 粗估（CJK 一字一 token）、按各供应商价格计费（$/1K）、LlmUsage 按天统计、超限明确拒绝、管理页实时显示（今日调用/tokens/花费/累计）
- **问答历史记录**：ChatSession/ChatMessage 持久化、📜 列表/继续对话/＋新对话/删除
- **小人 LLM 回答气泡**：回答流式显示在模型上方（15 秒淡出）、showChatReply 开关、无模型时降级锚点
- **小人设置**：管理页启用/回答气泡/隐藏自带蓝色气泡三项开关，保存即生效
- 修复：管理页上传记录/标签计数刷新归零；书架「全部」与筛选计数混淆（新增 allTotal）

## 验证
- VERIFY-ALL-PASS；真实用量已记录（6 次调用 / 2186 tokens / $0.0009）
