# Phase 3 · 撤销 · 小人 · 安全

**日期**：2026-08-14

## 内容
- **Ctrl+Z 撤销**：ArticleRevision 版本快照（更新/删除前自动保存）、`POST /api/articles/:id/undo`（回滚/删除重建）、撤销悬浮条、多步连续撤销、输入框内不拦截
- **动态小人**：oh-my-live2d 懒加载 + CDN 模型 + 本地模型优先 + CSS 降级小人；欢迎/深夜提示、点击气泡；🙈/🐣 收起召唤；⚖ 尺寸滑块（0.5~1.8x）
- **安全加固**：管理口令明文启动时自动转 scrypt 哈希写回、登录限流（10 分钟 5 次 → 429）、CORS 按 allowedOrigins 白名单
- **私密保护**：文章可标记 🔒 私密（AI 不可读、不进知识库）；所有提示词最前注入安全规则（防套话/防提示词注入）

## 验证
- 多步撤销、删除重建、429 限流、scrypt 哈希自检全部通过
